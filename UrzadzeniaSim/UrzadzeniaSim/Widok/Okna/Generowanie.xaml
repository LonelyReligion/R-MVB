<Window x:Class="UrzadzeniaSim.Widok.Okna.Generowanie"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UrzadzeniaSim.Widok.Okna"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="Generowanie" Height="280" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="10,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="ID: "/>
            <Label Grid.Column="1" Content="{Binding id}"/>
        </Grid>

        <Grid Grid.Row="1" Margin="10,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Generuj pomiar co "/>
            <xctk:IntegerUpDown Grid.Column="1" Watermark="ile?" Minimum="1" x:Name="sekundy" ValueChanged="sekundy_ValueChanged"/>
            <Label Grid.Column="2" Content=" sekund."/>
        </Grid>

        <Grid Grid.Row="2" Margin="10,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition />
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <RadioButton x:Name="generowanieZparemetryzowane"  GroupName="automatyczneGenerowanie" IsChecked="True" Grid.Column="0" VerticalAlignment="Center"/>
            <Label Grid.Column="1" Content="Zakończ generowanie automatycznie po "/>
            <xctk:IntegerUpDown Grid.Column="2" Watermark="ilu?" Minimum="1" IsEnabled="{Binding IsChecked, ElementName=generowanieZparemetryzowane}" 
                                x:Name="liczbaCykli" ValueChanged="sekundy_ValueChanged"/>
            <Label Grid.Column="3" Content=" cyklach."/>
        </Grid>

        <Grid Grid.Row="3" Margin="10,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <RadioButton x:Name="generowanieCykliczne"  GroupName="automatyczneGenerowanie" IsChecked="False" Grid.Column="0" VerticalAlignment="Center"/>
            <Label Grid.Column="1" Content="Generuj pomiary cyklicznie."/>
        </Grid>

        <Grid Grid.Row="4" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button x:Name="Stop" Grid.Column="0" Content="Stop" Width="100" Click="Stop_Click" IsEnabled="False"/>
            <Button x:Name="Start" Grid.Column="1" Content="Start" Width="100" IsEnabled="False" Click="Start_Click"/>
        </Grid>
        <ProgressBar Grid.Row="5" Height="20" Margin="0,20,0,0" x:Name="PasekPostepu" Foreground="#5C7D60"/>
    </Grid>
</Window>
